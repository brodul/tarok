class Card(object):

   @classmethod
      def calculate_round_winner(cls, cards, game_type):
            # glede na tip igre, pogledaš rang kart in dobiš zmagovalno
            #       return # vrneš objekt, s katerim boš lajlažje operiral dalje, npr če znaš za karto povedat h kateremu igralcu spada, bi js vrnil kr karto samo
            #
            #
            #       # Primer
            #
            #       >>> cards = [Card('AS'), Card('XII'), ...]
            #       >>> print Card.calculate_round_winner(cards, config.BERAC)
            #       >>> <Card 'AS'>
